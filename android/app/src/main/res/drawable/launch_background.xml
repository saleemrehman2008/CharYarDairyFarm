<?xml version="1.0" encoding="utf-8"?>
<!-- The window the OS shows before Flutter's first frame. Painting it the app's
     own background colour means no white flash on a cold start. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
</layer-list>
