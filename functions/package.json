{
  "name": "char-yar-dairy-farm-functions",
  "description": "Google Sheets mirror, push notifications and daily orders for Char Yar Dairy Farm.",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.3.2",
    "googleapis": "^180.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
